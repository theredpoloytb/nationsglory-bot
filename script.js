// ═══════════════════════════════════════════════════════════
// PASSWORD GATE — protection renforcée
// ═══════════════════════════════════════════════════════════
(function(){
  const SESSION_KEY='mg_auth_v2';
  const MAIN=document.querySelector('.main');
  const HDR=document.querySelector('.hdr');
  const NAV=document.querySelector('.nav');
  function lockContent(){if(MAIN)MAIN.style.display='none';if(HDR)HDR.style.display='none';if(NAV)NAV.style.display='none';}
  function unlockContent(){const l=document.getElementById('init-lock');if(l)l.remove();if(MAIN)MAIN.style.display='';if(HDR)HDR.style.display='';if(NAV)NAV.style.display='';}
  function trollUser(){lockContent();document.body.innerHTML='<div style="position:fixed;inset:0;background:#000;display:flex;flex-direction:column;align-items:center;justify-content:center;font-family:monospace;color:#f0c040;text-align:center;gap:2rem;z-index:99999"><div style="font-size:5rem">🕵️</div><div style="font-size:1.6rem;letter-spacing:.2em">INTRUSION DÉTECTÉE</div><div style="font-size:.85rem;color:rgba(240,192,64,.5);max-width:420px;line-height:1.8">La tentative de contournement a été enregistrée.<br>Accès définitivement révoqué.</div><div style="font-size:.7rem;color:rgba(200,100,100,.4)" id="fake-ip">Identification...</div><div style="font-size:.6rem;color:rgba(240,192,64,.2);margin-top:1rem">מוסד גלורי — CLASSIFIED</div></div>';setTimeout(()=>{document.getElementById('fake-ip').textContent=`Adresse : ${Math.floor(Math.random()*254)+1}.${Math.floor(Math.random()*255)}.${Math.floor(Math.random()*255)}.${Math.floor(Math.random()*254)+1} — signalement...`;},1800);}
  if(sessionStorage.getItem(SESSION_KEY)==='ok'){const g=document.getElementById('pw-gate');if(g)g.style.display='none';unlockContent();}else{lockContent();setTimeout(()=>{const e=document.getElementById('pw-input-el');if(e)e.focus();},100);const o=setInterval(()=>{const g=document.getElementById('pw-gate');if(!g&&sessionStorage.getItem(SESSION_KEY)!=='ok')trollUser();},500);window._stopGateWatch=()=>clearInterval(o);}
  window.pwCheck=async function(){const i=document.getElementById('pw-input-el'),e=document.getElementById('pw-err'),v=i.value;if(!v)return;try{const r=await fetch('https://nationsglory-spy.onrender.com/api/auth-check',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:v})}),d=await r.json();if(d.ok){sessionStorage.setItem(SESSION_KEY,'ok');if(window._stopGateWatch)window._stopGateWatch();unlockContent();const g=document.getElementById('pw-gate');g.style.transition='opacity .6s';g.style.opacity='0';setTimeout(()=>{g.style.display='none';},600);}else{e.style.opacity='1';i.value='';setTimeout(()=>{e.style.opacity='0';},2500);}}catch(e){err.textContent='ERREUR SERVEUR';err.style.opacity='1';setTimeout(()=>{err.style.opacity='0';err.textContent='CODE INVALIDE';},2500);}};
})();

// ═══════════════════════════════════════════════════════════
// MAIN APPLICATION
// ═══════════════════════════════════════════════════════════
const API='https://nationsglory-spy.onrender.com';
const SRV=["blue","coral","orange","red","yellow","mocha","white","jade","black","cyan","lime"];
const EMO={blue:"🔵",coral:"🔴",orange:"🟠",red:"🔴",yellow:"🟡",mocha:"🟤",white:"⚪",jade:"🟢",black:"⚫",cyan:"🔵",lime:"🟢"};
const BUG=s=>s==='red'||s==='mocha';
let WL=[],WLM=[],cwl='lime',snd=localStorage.getItem('mg_sound')!=='off';
let ALR=[],prev={},hist=JSON.parse(localStorage.getItem('mg_h')||'[]'),cc={},oP=[];
const $=id=>document.getElementById(id);
const ld=()=>`<div class="ld">Chargement<span class="ldd"><span>.</span><span>.</span><span>.</span></span></div>`;
const rP=(i,p)=>p.find(x=>x.toLowerCase()===i.toLowerCase())||i;

function drawSpark(canvasId,data,color='rgba(200,168,75,.5)'){
  const c=$(canvasId);if(!c||!data.length)return;
  const W=c.offsetWidth,H=c.offsetHeight;c.width=W*devicePixelRatio;c.height=H*devicePixelRatio;
  const ctx=c.getContext('2d');ctx.scale(devicePixelRatio,devicePixelRatio);
  if(data.length<2)return;
  const mn=Math.min(...data),mx=Math.max(...data),range=mx-mn||1;
  ctx.beginPath();
  data.forEach((v,i)=>{const x=i/(data.length-1)*W,y=H-(v-mn)/range*H*.8-H*.1;i?ctx.lineTo(x,y):ctx.moveTo(x,y);});
  ctx.strokeStyle=color;ctx.lineWidth=1;ctx.stroke();
  const grad=ctx.createLinearGradient(0,0,0,H);grad.addColorStop(0,'rgba(200,168,75,.1)');grad.addColorStop(1,'transparent');
  ctx.lineTo(W,H);ctx.lineTo(0,H);ctx.closePath();ctx.fillStyle=grad;ctx.fill();
}

// Background animation
(()=>{const c=$('bg'),ctx=c.getContext('2d');const sz=()=>{c.width=innerWidth;c.height=innerHeight};sz();window.addEventListener('resize',sz);const pts=Array.from({length:55},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.2,vy:(Math.random()-.5)*.2,r:Math.random()*.9+.2,a:Math.random()*.2+.04}));let mx=innerWidth/2,my=innerHeight/2,t=0;document.addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY});const orbs=[[.14,.1,200,168,75,.042,.44],[.86,.84,0,187,255,.022,.38],[.5,.44,157,111,255,.016,.32]];const draw=()=>{t+=.003;ctx.clearRect(0,0,c.width,c.height);orbs.forEach(([ox,oy,r,g,b,a,s])=>{const px=c.width*(ox+Math.sin(t*.6+ox*10)*.038),py=c.height*(oy+Math.cos(t*.42+oy*8)*.028);const grd=ctx.createRadialGradient(px,py,0,px,py,c.width*s);grd.addColorStop(0,`rgba(${r},${g},${b},${a})`);grd.addColorStop(1,'transparent');ctx.fillStyle=grd;ctx.fillRect(0,0,c.width,c.height);});pts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0)p.x=c.width;if(p.x>c.width)p.x=0;if(p.y<0)p.y=c.height;if(p.y>c.height)p.y=0;const d=Math.hypot(p.x-mx,p.y-my),br=d<90?1-d/90:0;ctx.beginPath();ctx.arc(p.x,p.y,p.r+br*1.2,0,Math.PI*2);ctx.fillStyle=`rgba(200,168,75,${p.a+br*.16})`;ctx.fill();});for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){const d=Math.hypot(pts[i].x-pts[j].x,pts[i].y-pts[j].y);if(d<80){ctx.beginPath();ctx.moveTo(pts[i].x,pts[i].y);ctx.lineTo(pts[j].x,pts[j].y);ctx.strokeStyle=`rgba(200,168,75,${.028*(1-d/80)})`;ctx.lineWidth=.5;ctx.stroke();}}requestAnimationFrame(draw);};draw();})();

// Cursor trail
(()=>{const cur=$('cur'),dot=$('cd'),tc=$('trl'),tc2=tc.getContext('2d');const sync=()=>{tc.width=innerWidth;tc.height=innerHeight};sync();window.addEventListener('resize',sync);let trail=[],MAX=16;document.addEventListener('mousemove',e=>{dot.style.left=e.clientX+'px';dot.style.top=e.clientY+'px';trail.push({x:e.clientX,y:e.clientY,t:Date.now()});if(trail.length>MAX)trail.shift();});document.addEventListener('mousedown',()=>cur.classList.add('c'));document.addEventListener('mouseup',()=>cur.classList.remove('c'));const draw=()=>{tc2.clearRect(0,0,tc.width,tc.height);const now=Date.now();trail.forEach((p,i)=>{const s=(i+1)/MAX*(1-(now-p.t)/200);if(s<=0)return;tc2.beginPath();tc2.arc(p.x,p.y,1.2*s,0,Math.PI*2);tc2.fillStyle=`rgba(240,192,64,${s*.16})`;tc2.fill();});requestAnimationFrame(draw);};draw();})();

setInterval(()=>{$('clock').textContent=new Date().toLocaleTimeString('fr-FR');$('hdr-date').textContent=new Date().toLocaleDateString('fr-FR');},1000);

function showToast(msg,duration=3000){const w=$('toast-wrap'),t=document.createElement('div');t.className='toast';t.textContent=msg;w.appendChild(t);setTimeout(()=>{t.style.animation='toastOut .3s forwards';setTimeout(()=>t.remove(),300);},duration);}

document.addEventListener('keydown',e=>{if(e.target.tagName==='INPUT'||e.target.tagName==='SELECT')return;const tabs=document.querySelectorAll('.tab');const map={'1':0,'2':1,'3':2,'4':3,'5':4,'6':5,'7':6,'8':7,'9':8};if(map[e.key]!==undefined&&tabs[map[e.key]]){tabs[map[e.key]].click();showToast(`Onglet ${e.key}`);return;}if(e.key==='/'||e.key==='k'&&(e.ctrlKey||e.metaKey)){e.preventDefault();tabs[2].click();setTimeout(()=>$('ca-input').focus(),300);}});

// Loader animation
let pct=0;
const pctT=setInterval(()=>{pct=Math.min(pct+(Math.random()*8+2),92);if($('l-pct'))$('l-pct').textContent=Math.floor(pct)+'%';},85);
const msgs=['CHARGEMENT...','CONNEXION API...','SYNCHRONISATION...','VÉRIFICATION...','SYSTÈME PRÊT'];
let mi=0;
const msgT=setInterval(()=>{if(mi<msgs.length-1&&$('l-msg'))$('l-msg').textContent=msgs[mi++];},500);
window.addEventListener('load',()=>{clearInterval(pctT);clearInterval(msgT);if($('l-pct'))$('l-pct').textContent='100%';setTimeout(()=>{const m=$('l-msg');if(m){m.textContent='SYSTÈME PRÊT — CLIQUEZ POUR ENTRER';m.classList.remove('blink');m.classList.add('rdy');}const b=$('lbtn');if(b)b.style.display='block';},300);});
function enterSite(){try{if(!actx)actx=new(window.AudioContext||window.webkitAudioContext)();actx.resume();}catch(e){}_au=true;window.scrollTo(0,0);$('ldr').classList.add('out');setTimeout(()=>{$('ldr').style.display='none';showToast('SYSTÈME OPÉRATIONNEL',2500);},900);}

// SoundCloud player
let scW=null,scOn=false,scBarT=null;
(()=>{const ifr=document.createElement('iframe');ifr.allow='autoplay';ifr.style.cssText='position:absolute;width:0;height:0;border:none;opacity:0';ifr.src='https://w.soundcloud.com/player/?url=https%3A//soundcloud.com/user-164072391-103154989/omer-adam-feat-arisa-tel-aviv&color=%23f0c040&auto_play=false&hide_related=true';document.body.appendChild(ifr);const s=document.createElement('script');s.src='https://w.soundcloud.com/player/api.js';s.onload=()=>{scW=SC.Widget(ifr);scW.bind(SC.Widget.Events.READY,()=>scW.setVolume(70));scW.bind(SC.Widget.Events.PLAY,()=>{scOn=true;$('scp-tri').classList.add('p');$('scp-eq').classList.add('on');scStartBar();});scW.bind(SC.Widget.Events.PAUSE,()=>{scOn=false;$('scp-tri').classList.remove('p');$('scp-eq').classList.remove('on');scStopBar();});scW.bind(SC.Widget.Events.FINISH,()=>{scOn=false;$('scp-tri').classList.remove('p');$('scp-eq').classList.remove('on');$('scp-bf').style.width='0%';});};document.head.appendChild(s);})();
function scToggle(){if(!scW)return;scOn?scW.pause():scW.play();}
function scVol(v){if(scW)scW.setVolume(parseInt(v));}
function scStartBar(){scStopBar();scBarT=setInterval(()=>{if(!scW)return;scW.getPosition(p=>{scW.getDuration(d=>{if(d>0)$('scp-bf').style.width=(p/d*100)+'%';});});},500);}
function scStopBar(){if(scBarT){clearInterval(scBarT);scBarT=null;}}

// API functions
async function api(p){const r=await fetch(API+p);if(!r.ok)throw new Error('HTTP '+r.status);return r.json();}
async function apiP(p,b){const r=await fetch(API+p,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});if(!r.ok)throw new Error('HTTP '+r.status);return r.json();}

// Navigation
async function nav(id,btn){sndNav();pageFlash();document.querySelectorAll('.sec').forEach(s=>s.classList.remove('active'));document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));$('s-'+id).classList.add('active');btn.classList.add('active');if(id==='watchlist')await switchWl('lime');if(id==='countrywatch'){cwRender();cwRefreshAll();}if(id==='online'){$('ol-body').innerHTML=ld();loadOnline();}if(id==='checkall')rAT('ca-pl','ppCA');if(id==='stats')rAT('st-pl','ppST');}
function rAT(id,fn){const e=$(id);if(!e||!oP.length)return;e.innerHTML=oP.map(p=>`<span class="tag" onclick="${fn}('${p.replace(/'/g,"\\'")}')">${p}</span>`).join('');}
function fPT(ii,di){const e=$(di);if(!e)return;const v=$(ii).value.trim().toLowerCase(),f=v?oP.filter(p=>p.toLowerCase().includes(v)):oP;if(!f.length){e.innerHTML='';return;}const m={'ca-pl':'ppCA','st-pl':'ppST','wl-pl':'ppWL'};e.innerHTML=f.slice(0,100).map(p=>`<span class="tag" onclick="${m[di]||'qCA'}('${p.replace(/'/g,"\\'")}')">${p}</span>`).join('');}
function ppCA(p){$('ca-input').value=p;$('ca-pl').innerHTML='';$('ca-list').style.display='none';doCA();}
function ppST(p){$('st-input').value=p;$('st-pl').innerHTML='';$('st-list').style.display='none';loadStats();}
function ppWL(p){$('wl-add').value=p;$('wl-pl').innerHTML='';$('wl-acl').style.display='none';}
function acF(ii,li,pool){const v=$(ii).value.trim().toLowerCase(),l=$(li);if(!v||!pool.length){l.style.display='none';return;}const m=pool.filter(p=>p.toLowerCase().includes(v)).slice(0,10);if(!m.length){l.style.display='none';return;}l.innerHTML=m.map(p=>`<div class="aci" onmousedown="acP('${ii}','${li}','${p.replace(/'/g,"\\'")}')">${p}</div>`).join('');l.style.display='block';}
function acFC(){const s=$('ck-srv').value;if(!s)return;const v=$('ck-country').value.trim().toLowerCase(),p=cc[s]||[];$('ck-suggest').innerHTML=(v?p.filter(c=>c.toLowerCase().includes(v)):p).map(c=>`<span class="tag" onclick="selC('${c.replace(/'/g,"\\'")}')">${c}</span>`).join('');acF('ck-country','ck-list',p);}
function acP(i,l,v){$(i).value=v;$(l).style.display='none';}
function acK(e,l,cb){const list=$(l),items=list.querySelectorAll('.aci'),cur=list.querySelector('.sel');if(e.key==='ArrowDown'){e.preventDefault();const n=cur?cur.nextElementSibling:items[0];if(cur)cur.classList.remove('sel');if(n)n.classList.add('sel');}else if(e.key==='ArrowUp'){e.preventDefault();const p=cur?cur.previousElementSibling:items[items.length-1];if(cur)cur.classList.remove('sel');if(p)p.classList.add('sel');}else if(e.key==='Enter'){if(cur){cur.onmousedown();return;}list.style.display='none';cb();}else if(e.key==='Escape')list.style.display='none';}
document.addEventListener('click',e=>{document.querySelectorAll('.acl').forEach(l=>{if(!l.parentElement.contains(e.target))l.style.display='none';});});

// Data loading
async function chkAPI(){try{await api('/health');$('api-led').className='led on';$('api-txt').textContent='API OK';return true;}catch{$('api-led').className='led off';$('api-txt').textContent='DOWN';return false;}}
async function loadWL(){try{const c=new AbortController(),t=setTimeout(()=>c.abort(),6000);const r=await fetch(API+'/api/watchlist',{signal:c.signal});clearTimeout(t);const d=await r.json();WL=d.players||[];animStat('st-wcount',WL.length);drawSpark('spark-wc',sparkData.wc);}catch{}}
async function loadWLM(){try{const c=new AbortController(),t=setTimeout(()=>c.abort(),6000);const r=await fetch(API+'/api/watchlist_mocha',{signal:c.signal});clearTimeout(t);const d=await r.json();WLM=d.players||[];}catch{}}
async function loadKP(){try{const d=await fetch(API+'/api/known_players').then(r=>r.json());oP=[...new Set([...(d.players||[]),...oP])].sort((a,b)=>a.toLowerCase().localeCompare(b.toLowerCase()));rAT('ca-pl','ppCA');rAT('st-pl','ppST');rAT('wl-pl','ppWL');}catch{}}
function animStat(id,val){const el=$(id);if(!el)return;const prev=parseInt(el.textContent)||0;if(prev===val)return;el.style.opacity='.12';setTimeout(()=>{el.textContent=val;el.style.opacity='1';el.classList.add('bump');setTimeout(()=>el.classList.remove('bump'),350);},120);}

const sparkData={total:[],wl:[],wc:[]};
async function loadDash(){if(!$('srv-overview').children.length)$('srv-overview').innerHTML=ld();try{const all=await api('/api/online_all'),lp=all['lime']||[];const pool=new Set(oP);SRV.forEach(s=>(all[s]||[]).forEach(p=>pool.add(p)));oP=[...pool].sort((a,b)=>a.toLowerCase().localeCompare(b.toLowerCase()));WL.forEach(p=>{const lc=p.toLowerCase(),on=(all['lime']||[]).map(x=>x.toLowerCase()).includes(lc),k=p+'@lime';if(on&&!prev[k])pAlert('connect',p,'lime');if(!on&&prev[k])pAlert('disconnect',p,'lime');prev[k]=on;});WLM.forEach(p=>{const lc=p.toLowerCase(),on=(all['mocha']||[]).map(x=>x.toLowerCase()).includes(lc),k=p+'@mocha';if(on&&!prev[k])pAlert('connect',p,'mocha');if(!on&&prev[k])pAlert('disconnect',p,'mocha');prev[k]=on;});let tot=0;SRV.forEach(s=>tot+=(all[s]||[]).length);animStat('st-total',tot);sparkData.total.push(tot);if(sparkData.total.length>30)sparkData.total.shift();drawSpark('spark-total',sparkData.total);const wonline=WL.filter(p=>lp.map(x=>x.toLowerCase()).includes(p.toLowerCase())).length;animStat('st-wonline',wonline);updateWeather(wonline);sparkData.wl.push(wonline);if(sparkData.wl.length>30)sparkData.wl.shift();drawSpark('spark-wl',sparkData.wl);const mx=Math.max(...SRV.map(s=>(all[s]||[]).length),1);const cards=$('srv-overview').querySelectorAll('.sc');if(cards.length===SRV.length){SRV.forEach((s,i)=>{const cnt=(all[s]||[]).length,c=cards[i],n=c.querySelector('.sc-n'),b=c.querySelector('.sbar-f');if(n&&parseInt(n.textContent)!==cnt){n.style.opacity='.1';setTimeout(()=>{n.textContent=cnt;n.style.opacity='1';},120);}if(b)b.style.width=Math.round(cnt/mx*100)+'%';});}else $('srv-overview').innerHTML=SRV.map(s=>{const cnt=(all[s]||[]).length;return`<div class="sc" onclick="gOL('${s}')"><div class="sc-top"><span class="sc-name">${s.toUpperCase()}</span><span class="sc-emo">${EMO[s]}</span></div><div class="sc-n">${cnt}</div><div class="sc-lbl">EN LIGNE</div><div class="sbar"><div class="sbar-f" style="width:${Math.round(cnt/mx*100)}%"></div></div></div>`;}).join('');const mp=await api('/api/online/mocha').then(d=>d.players||[]).catch(()=>[]);const mk=(pl,l,c,lb)=>l.length?`<div style="font-family:var(--M);font-size:.46rem;color:${c};margin-bottom:.18rem">${lb}</div><div class="tags" style="margin-bottom:.35rem">${l.map(p=>{const on=pl.map(x=>x.toLowerCase()).includes(p.toLowerCase());return`<span class="tag ${on?'on':''}" onclick="openPlayerPanel('${p}')">${on?'🟢':'⚫'} ${p}</span>`;}).join('')}</div>`:'';$('wl-quick').innerHTML=(mk(lp,WL,'var(--grn)','🟢 LIME')||'')+(mk(mp,WLM,'var(--org)','🟤 MOCHA')||'')||'<div class="empty">Watchlists vides</div>';if($('last-update'))$('last-update').textContent=new Date().toLocaleTimeString('fr-FR');pushActivity(tot);startCountdown(5);if(document.getElementById('wl-status')&&document.getElementById('wl-status').innerHTML.trim()!=='')wlRS();}catch(e){$('srv-overview').innerHTML=`<div class="empty" style="color:var(--red)">Bot hors ligne<br/><span style="font-size:.52rem;opacity:.6">${e.message}</span></div>`;}}
function pAlert(t,p,s){ALR.unshift({type:t,player:p,server:s,time:new Date().toLocaleTimeString('fr-FR')});if(ALR.length>60)ALR.pop();rAlerts();const inWL=WL.map(x=>x.toLowerCase()).includes(p.toLowerCase())||WLM.map(x=>x.toLowerCase()).includes(p.toLowerCase());if(inWL){if(t==='connect'){setLastSeen(p,s);startSession(p);sendBrowserNotif('connect',p,s);}else{endSession(p);sendBrowserNotif('disconnect',p,s);}showPop(t,p,s);}}
function rAlerts(){$('alert-badge').textContent=ALR.length+' évts';$('alert-feed').innerHTML=ALR.length?ALR.map(a=>`<div class="fi"><div class="fi-d ${a.type==='connect'?'g':'r'}"></div><span class="fi-t">${a.time}</span><span class="fi-m">${a.type==='connect'?'🟢':'🔴'} <b onclick="openPlayerPanel('${a.player}')">${a.player}</b><span class="fi-s">${a.server.toUpperCase()}</span></span></div>`).join(''):'<div class="empty">En attente...</div>';}

function gOL(s){document.querySelectorAll('.tab')[1].click();$('ol-srv').value=s;$('ol-body').innerHTML=ld();loadOLS(s);}
async function loadOLS(srv){const body=$('ol-body');try{const d=await api('/api/online/'+srv),pl=d.players||[];body.innerHTML=(pl.length?`<div class="tags">${pl.map(p=>`<span class="tag ${WL.map(w=>w.toLowerCase()).includes(p.toLowerCase())?'on':''}" onclick="openPlayerPanel('${p}')">${p}</span>`).join('')}</div>`:`<div class="empty">Aucun joueur</div>`);}catch(e){body.innerHTML=`<div class="empty">Erreur : ${e.message}</div>`;}}
async function loadOnline(){const srv=$('ol-srv').value,body=$('ol-body');body.innerHTML=ld();try{if(!srv){const all=await api('/api/online_all');let tot=0;SRV.forEach(s=>tot+=(all[s]||[]).length);body.innerHTML=`<div style="margin-bottom:.55rem">TOTAL <span style="color:var(--gb)">${tot}</span> joueurs</div><div class="sg">${SRV.map(s=>{const pl=all[s]||[];return`<div class="sc" onclick="fOL('${s}')"><div class="sc-top"><span class="sc-name">${s.toUpperCase()}</span><span class="sc-emo">${EMO[s]}</span></div><div class="sc-n">${pl.length}</div><div class="sc-lbl">CLIQUER</div><div class="tags">${pl.slice(0,16).map(p=>`<span class="tag" onclick="event.stopPropagation();qCA('${p}')">${p}</span>`).join('')}</div><div class="sbar"><div class="sbar-f" style="width:${Math.round(pl.length/Math.max(...SRV.map(ss=>(all[ss]||[]).length),1)*100)}%"></div></div></div>`;}).join('')}</div>`;}else await loadOLS(srv);}catch(e){body.innerHTML=`<div class="empty">Erreur : ${e.message}</div>`;}}

async function doCA(){const raw=$('ca-input').value.trim();if(!raw)return;const p=rP(raw,oP);$('ca-input').value=p;const res=$('ca-result');res.innerHTML=ld();try{const d=await api('/api/checkall/'+encodeURIComponent(p));if(d.servers.length)sndF();res.innerHTML=d.servers.length?`<div class="res ok"><div class="rt">Joueur localisé — ${p}</div>${d.servers.map(s=>`<div>${EMO[s]} <span style="color:var(--g)">${s.toUpperCase()}</span></div>`).join('')}</div>`:`<div class="res err"><div class="rt">Résultat</div><span>${p} n'est connecté nulle part</span></div>`;hist=hist.filter(h=>h.p.toLowerCase()!==p.toLowerCase());hist.unshift({p,servers:d.servers,t:new Date().toLocaleTimeString('fr-FR')});if(hist.length>15)hist.pop();localStorage.setItem('mg_h',JSON.stringify(hist));rHist();}catch(e){res.innerHTML=`<div class="res err">Erreur : ${e.message}</div>`;}}
function qCA(player){openPlayerPanel(player);}
function rHist(){const el=$('ca-history');if(!hist.length){el.innerHTML='<div class="empty">Aucune recherche</div>';return;}el.innerHTML=`<table class="tbl"><thead><tr><th>Joueur</th><th>Serveurs</th><th>Heure</th></tr></thead><tbody>`+hist.slice(0,12).map(h=>`<tr><td onclick="qCA('${h.p}')">${h.p}</td><td>${h.servers.length?h.servers.map(s=>`<span class="stag">${s.toUpperCase()}</span>`).join(''):'Hors ligne'}</td><td>${h.t}</td></tr>`).join('')+'</tbody></table>';}

async function loadCS(){const s=$('ck-srv').value;if(!s)return;$('ck-suggest').innerHTML=`<span>Chargement...</span>`;try{if(!cc[s]){const d=await api('/api/countries/'+s);cc[s]=(d.countries||[]).sort((a,b)=>a.toLowerCase().localeCompare(b.toLowerCase()));}rCS(s);}catch{cc[s]=[];$('ck-suggest').innerHTML='';}}
function rCS(s){const el=$('ck-suggest'),p=cc[s]||[];if(!p.length){el.innerHTML='';return;}el.innerHTML=p.map(c=>`<span class="tag" onclick="selC('${c.replace(/'/g,"\\'")}')">${c}</span>`).join('');}
function selC(c){$('ck-country').value=c;$('ck-list').style.display='none';doCheck();}
async function doCheck(){const s=$('ck-srv').value,raw=$('ck-country').value.trim();if(!s||!raw)return;const c=rP(raw,cc[s]||[]);$('ck-country').value=c;const res=$('ck-result');res.innerHTML=ld();try{const d=await api(`/api/check/${s}/${encodeURIComponent(c)}`);if(d.online_total===0){res.innerHTML=`<div class="res ok"><div class="rt">Pays : ${d.country} — ${d.members_total} membres</div><span>✓ Aucun membre connecté</span></div>`;}else{res.innerHTML=`<div class="res err"><div class="rt">Pays : ${d.country} — ${d.online_total}/${d.members_total} connectés</div>`+Object.entries(d.servers).sort((a,b)=>a[0]===s?-1:1).map(([x,pl])=>`<div>${EMO[x]} <span>${x.toUpperCase()}</span> <span>${pl.join(', ')}</span></div>`).join('')+'</div>';}}catch(e){res.innerHTML=`<div class="res err"><div class="rt">Erreur</div>${e.message}</div>`;}}

function wlR(){const el=$('wl-manage'),wl=cwl==='mocha'?WLM:WL;if(!wl.length){el.innerHTML='<div class="empty">Watchlist vide</div>';return;}el.innerHTML=wl.map(p=>`<div class="wi"><span>${p}</span><button class="btn btn-r" onclick="wlRm('${p}')">✕</button></div>`).join('');}
async function wlAdd(){const raw=$('wl-add').value.trim();if(!raw)return;const name=rP(raw,oP);$('wl-add').value='';try{const d=await apiP(cwl==='mocha'?'/api/watchlist_mocha/add':'/api/watchlist/add',{player:name});if(cwl==='mocha')WLM=d.players;else WL=d.players;wlR();wlRS();showToast(`${name} ajouté`);}catch(e){showToast('Erreur : '+e.message);}}
async function wlRm(name){try{const d=await apiP(cwl==='mocha'?'/api/watchlist_mocha/remove':'/api/watchlist/remove',{player:name});if(cwl==='mocha')WLM=d.players;else WL=d.players;wlR();wlRS();showToast(`${name} retiré`);}catch(e){showToast('Erreur : '+e.message);}}
async function switchWl(s){cwl=s;document.querySelectorAll('.wtb').forEach(e=>e.classList.remove('act'));const a=$('wl-tab-'+s);if(a)a.classList.add('act');if($('wl-pt'))$('wl-pt').textContent='◈ Watchlist — '+s.toUpperCase();if($('wl-st'))$('wl-st').textContent='⚡ Statut live — '+s.toUpperCase();$('wl-manage').innerHTML='';$('wl-status').innerHTML='';if(s==='lime')await loadWL();else await loadWLM();wlR();wlRS();}
async function wlRS(){const el=$('wl-status');if(!el)return;el.innerHTML=ld();const s=cwl==='mocha'?'mocha':'lime',wl=cwl==='mocha'?WLM:WL;if(!wl.length){el.innerHTML='<div class="empty">Watchlist vide</div>';return;}try{const c=new AbortController(),t=setTimeout(()=>c.abort(),8000);const r=await fetch(API+'/api/online/'+s,{signal:c.signal});clearTimeout(t);const d=await r.json(),lp=d.players||[];const on=wl.filter(p=>lp.map(x=>x.toLowerCase()).includes(p.toLowerCase()));const off=wl.filter(p=>!lp.map(x=>x.toLowerCase()).includes(p.toLowerCase()));on.forEach(p=>{setLastSeen(p,s);loadSessionDurations(p);});el.innerHTML=[...on.map(p=>`<div class="wi" onclick="openPlayerPanel('${p}')"><span>${p}</span><div class="wis on"><div class="led on"></div>EN LIGNE</div><span class="session-timer" data-player="${p}">${getSessionTime(p)||''}</span></div>`),...off.map(p=>`<div class="wi" onclick="openPlayerPanel('${p}')"><span>${p}</span><div class="wis off">◯ Hors ligne</div></div>`)] .join('')||'<div class="empty">Aucune donnée</div>';}catch{el.innerHTML=`<div class="empty">Erreur</div>`;}}

async function loadStats(){const raw=$('st-input').value.trim();if(!raw)return;const p=rP(raw,oP);$('st-input').value=p;const res=$('st-result');res.innerHTML=`<div class="panel mb"><div class="ph"><span class="pt">◐ Analyse — ${p}</span></div><div class="pb">${ld()}</div></div>`;const[lR,pR,plR]=await Promise.allSettled([api('/api/checkall/'+encodeURIComponent(p)),api('/api/pronostic/'+encodeURIComponent(p)),api('/api/plages/'+encodeURIComponent(p))]);const loc=lR.status==='fulfilled'?lR.value:null,prn=pR.status==='fulfilled'?pR.value:null,plg=plR.status==='fulfilled'?plR.value:null;const srvs=loc?loc.servers:[];let h='';h+=`<div class="sb"><div class="sbt">◉ Localisation</div>`;h+=srvs.length?srvs.map(s=>`<div class="ir"><span class="ik">Serveur</span><span class="iv ok">${s.toUpperCase()}</span></div>`).join(''):`<div class="ir"><span class="ik">Statut</span><span class="iv">Hors ligne</span></div>`;h+='</div>';h+=`<div class="sb"><div class="sbt">◐ Pronostic</div>`;if(prn?.pronostic?.length){h+=`<div>Basé sur ${prn.total} connexions</div>`;h+=prn.pronostic.map(r=>`<div class="pr"><span class="pd">${r.day}</span><div class="pb3"><div class="pbf" style="width:${r.pct}%"></div></div><span class="pp">${r.pct}%</span><span class="pt3">${r.avg_h}h${String(r.avg_m).padStart(2,'0')}</span></div>`).join('');}else h+=`<div class="ir"><span class="ik">Données</span><span class="iv">Insuffisantes</span></div>`;h+='</div>';h+=`<div class="sb"><div class="sbt">🕐 Heatmap</div>`;if(plg?.heatmap){const hm=plg.heatmap,days=plg.days,mx=Math.max(...hm.flat(),1);h+=`<div><table style="border-collapse:collapse;width:100%"><tr><td style="padding-right:.34rem">H→</td>`;for(let i=0;i<24;i++)h+=`<td style="text-align:center;padding:0 1px;font-size:.4rem">${i}</td>`;h+='</tr>';days.forEach((day,di)=>{h+=`<tr><td style="padding-right:.34rem">${day}</td>`;hm[di].forEach(v=>{const bg=v?`rgba(200,168,75,${(.04+v/mx*.82).toFixed(2)})`:'rgba(2,5,12,.9)';h+=`<td style="width:13px;height:11px;background:${bg};border-radius:1px;padding:0"></td>`;});h+='</tr>';});h+='</table></div>';}else h+=`<div class="ir"><span class="ik">Données</span><span class="iv">Aucune</span></div>`;h+='</div>';res.innerHTML=h;}

let actx=null,_au=false;
function gCtx(){if(!actx)actx=new(window.AudioContext||window.webkitAudioContext)();if(actx.state==='suspended')actx.resume();return actx;}
function unlockAudio(){if(_au)return;_au=true;try{gCtx().resume();}catch(e){}}
document.addEventListener('click',unlockAudio,{once:true});
function toggleSound(){snd=!snd;localStorage.setItem('mg_sound',snd?'on':'off');const b=$('sound-btn');if(b){b.textContent=snd?'🔊 SON':'🔇 SON';b.style.color=snd?'var(--g)':'var(--t3)';}showToast(snd?'Son activé':'Son désactivé');}
function pT(f,t='sine',d=.08,v=.08,dl=0){if(!snd)return;try{const ctx=gCtx(),o=ctx.createOscillator(),g=ctx.createGain();o.connect(g);g.connect(ctx.destination);o.type=t;o.frequency.value=f;const ts=ctx.currentTime+dl;g.gain.setValueAtTime(0,ts);g.gain.linearRampToValueAtTime(v,ts+.01);g.gain.exponentialRampToValueAtTime(.001,ts+d);o.start(ts);o.stop(ts+d);}catch(e){}}
function sndNav(){pT(880,'sine',.06,.055);pT(1100,'sine',.05,.038,.04);}
function sndH(){pT(660,'sine',.04,.018);}
function sndF(){pT(523,'sine',.08,.09);pT(659,'sine',.08,.09,.07);}
function sndA(c){if(c){[0,.14,.28,.42,.56].forEach((dl,i)=>{pT(580+i*85,'square',.11,.28,dl);pT(1160+i*110,'sine',.07,.12,dl+.05);});}else{[0,.17,.34].forEach((dl,i)=>{pT(490-i*95,'sawtooth',.14,.28,dl);pT(245-i*48,'square',.11,.18,dl+.07);});}}
function spk(t){if(!snd)return;try{window.speechSynthesis.cancel();const m=new SpeechSynthesisUtterance(t);m.lang='fr-FR';m.rate=.88;m.pitch=1.25;window.speechSynthesis.speak(m);}catch(e){}}
function showPop(type,player,server){sndA(type==='connect');setTimeout(()=>spk(type==='connect'?`${player} connecté`:`${player} déconnecté`),400);const pop=$('apop'),n=document.createElement('div');n.className='an'+(type==='disconnect'?' dc':'');n.innerHTML=`<div class="an-type">${type==='connect'?'🟢 Connexion':'🔴 Déconnexion'}</div><div class="an-name">${player}</div><div class="an-meta"><span>${server.toUpperCase()}</span><span>${new Date().toLocaleTimeString('fr-FR')}</span></div><span class="an-x" onclick="this.parentElement.remove()">✕</span>`;n.onclick=e=>{if(e.target.classList.contains('an-x'))return;qCA(player);};pop.appendChild(n);setTimeout(()=>{n.style.animation='anOut .28s forwards';setTimeout(()=>n.remove(),280);},8000);}
function pageFlash(){const f=document.getElementById('page-flash');f.classList.remove('go');void f.offsetWidth;f.classList.add('go');}
const actHistory=[],actLabels=[],MAX_ACT=30;
function pushActivity(total){const now=new Date();actHistory.push(total);actLabels.push(now.getHours()+':'+String(now.getMinutes()).padStart(2,'0'));if(actHistory.length>MAX_ACT){actHistory.shift();actLabels.shift();}drawActivityGraph();}
function drawActivityGraph(){const c=document.getElementById('activity-graph');if(!c||actHistory.length<2)return;const W=c.parentElement.offsetWidth||400,H=80;c.width=W*devicePixelRatio;c.height=H*devicePixelRatio;const ctx=c.getContext('2d');ctx.scale(devicePixelRatio,devicePixelRatio);const mn=Math.min(...actHistory),mx=Math.max(...actHistory,1),range=mx-mn||1;const pad=8,gW=W-pad*2,gH=H-pad*2;for(let i=0;i<=4;i++){const y=pad+gH*(1-i/4);ctx.beginPath();ctx.moveTo(pad,y);ctx.lineTo(W-pad,y);ctx.strokeStyle='rgba(200,168,75,.05)';ctx.lineWidth=1;ctx.stroke();}const grad=ctx.createLinearGradient(0,0,0,H);grad.addColorStop(0,'rgba(200,168,75,.18)');grad.addColorStop(1,'rgba(200,168,75,.01)');ctx.beginPath();actHistory.forEach((v,i)=>{const x=pad+i/(actHistory.length-1)*gW,y=pad+gH*(1-(v-mn)/range);i===0?ctx.moveTo(x,y):ctx.lineTo(x,y);});ctx.lineTo(pad+gW,H-pad);ctx.lineTo(pad,H-pad);ctx.closePath();ctx.fillStyle=grad;ctx.fill();ctx.beginPath();actHistory.forEach((v,i)=>{const x=pad+i/(actHistory.length-1)*gW,y=pad+gH*(1-(v-mn)/range);i===0?ctx.moveTo(x,y):ctx.lineTo(x,y);});ctx.strokeStyle='rgba(200,168,75,.65)';ctx.lineWidth=1.5;ctx.stroke();const lx=pad+gW,ly=pad+gH*(1-(actHistory[actHistory.length-1]-mn)/range);ctx.beginPath();ctx.arc(lx,ly,3,0,Math.PI*2);ctx.fillStyle='var(--gb)';ctx.fill();const lel=document.getElementById('graph-labels');if(lel&&actLabels.length>0){const step=Math.max(1,Math.floor(actLabels.length/5));const shown=actLabels.filter((_,i)=>i%step===0||i===actLabels.length-1);lel.innerHTML='';shown.forEach(l=>{const s=document.createElement('span');s.textContent=l;lel.appendChild(s);});}}
let cdTotal=5,cdLeft=5;
function startCountdown(total=5){cdTotal=total;cdLeft=total;updateCountdown();}
function updateCountdown(){const txt=document.getElementById('cd-txt'),fill=document.getElementById('cd-fill');if(txt)txt.textContent=cdLeft+'s';if(fill)fill.style.width=(cdLeft/cdTotal*100)+'%';}
function tickCountdown(){cdLeft=Math.max(0,cdLeft-1);updateCountdown();}
const lastSeen={};
function setLastSeen(player,server){lastSeen[player]={ts:Date.now(),server};}
let notifGranted=false;
async function requestNotifPerms(){if(!('Notification' in window))return;if(Notification.permission==='granted'){notifGranted=true;return;}if(Notification.permission!=='denied'){const p=await Notification.requestPermission();notifGranted=p==='granted';}}
function sendBrowserNotif(type,player,server){if(!notifGranted||document.visibilityState==='visible')return;try{const n=new Notification(type==='connect'?`🟢 ${player} connecté`:`🔴 ${player} déconnecté`,{body:`Serveur : ${server.toUpperCase()}`,icon:'https://nationsglory.fr/favicon.ico'});n.onclick=()=>{window.focus();n.close();};}catch(e){}}
let ppOpen=false;
async function openPlayerPanel(player){const panel=document.getElementById('player-panel'),overlay=document.getElementById('pp-overlay'),nameEl=document.getElementById('pp-name'),body=document.getElementById('pp-body');nameEl.textContent=player;body.innerHTML=`<div class="pp-loading">◌ Chargement...</div>`;panel.classList.add('open');overlay.classList.add('open');document.body.style.overflow='hidden';ppOpen=true;const[caR,prR,plR]=await Promise.allSettled([api('/api/checkall/'+encodeURIComponent(player)),api('/api/pronostic/'+encodeURIComponent(player)),api('/api/plages/'+encodeURIComponent(player))]);const ca=caR.status==='fulfilled'?caR.value:null,pr=prR.status==='fulfilled'?prR.value:null,pl=plR.status==='fulfilled'?plR.value:null;const online=ca&&ca.servers.length>0;let h='';h+=`<div class="pp-info-row"><div class="pp-avatar"><img src="https://mc-heads.net/avatar/${encodeURIComponent(player)}/64" onerror="this.style.display='none'"></div><div class="pp-info-meta"><div class="pp-status ${online?'on':'off'}"><div class="led ${online?'on':'off'}"></div>${online?ca.servers.map(s=>`${EMO[s]} ${s.toUpperCase()}`).join(' · '):'Hors ligne'}</div></div></div>`;h+=`<a class="pp-ng-link" href="https://nationsglory.fr/profile/${encodeURIComponent(player)}" target="_blank">↗ Profil NG</a>`;h+=`<div class="pp-section"><div class="pp-sec-title">◐ Pronostic</div>`;if(pr?.pronostic?.length){h+=`<div>Basé sur ${pr.total} connexions</div>`;h+=pr.pronostic.map(r=>`<div class="pr"><span class="pd">${r.day}</span><div class="pb3"><div class="pbf" style="width:${r.pct}%"></div></div><span class="pp">${r.pct}%</span></div>`).join('');}else h+=`<div>Pas assez de données</div>`;h+='</div>';if(pl?.heatmap){const hm=pl.heatmap,days=pl.days,mxH=Math.max(...hm.flat(),1);h+=`<div class="pp-section"><div class="pp-sec-title">🕐 Heatmap</div><div><table class="pp-heatmap">`;days.forEach((day,di)=>{h+=`<tr><td>${day}</td>`;hm[di].forEach(v=>{const bg=v?`rgba(200,168,75,${(.04+v/mxH*.82).toFixed(2)})`:'rgba(2,5,12,.9)';h+=`<td style="width:13px;height:11px;background:${bg}"></td>`;});h+='</tr>';});h+='</table></div></div>';}body.innerHTML=h;}
function closePlayerPanel(){document.getElementById('player-panel').classList.remove('open');document.getElementById('pp-overlay').classList.remove('open');document.body.style.overflow='';ppOpen=false;}
document.addEventListener('keydown',e=>{if(e.key==='Escape'&&ppOpen)closePlayerPanel();});

// Matrix canvas loader
(()=>{const c=document.getElementById('matrix-canvas');if(!c)return;const ctx=c.getContext('2d');const resize=()=>{c.width=window.innerWidth;c.height=window.innerHeight;};resize();window.addEventListener('resize',resize);const chars='アイウエオカキクケコ0123456789ABCDEF◈';const fs=14;let cols=Math.floor(c.width/fs);let drops=Array(cols).fill(1);const draw=()=>{ctx.fillStyle='rgba(1,4,8,.08)';ctx.fillRect(0,0,c.width,c.height);cols=Math.floor(c.width/fs);if(drops.length!==cols)drops=Array(cols).fill(1);ctx.font=fs+'px JetBrains Mono,monospace';drops.forEach((y,i)=>{const ch=chars[Math.floor(Math.random()*chars.length)];ctx.fillStyle=Math.random()>.92?'#f0c040':`rgba(200,168,75,${Math.random()*.6+.15})`;ctx.fillText(ch,i*fs,y*fs);if(y*fs>c.height&&Math.random()>.975)drops[i]=0;drops[i]++;});};const interval=setInterval(draw,45);const obs=new MutationObserver(()=>{const ldr=document.getElementById('ldr');if(ldr&&ldr.style.display==='none'){clearInterval(interval);obs.disconnect();}});const ldr=document.getElementById('ldr');if(ldr)obs.observe(ldr,{attributes:true,attributeFilter:['style']});})();

function updateWeather(onlineCount){const icon=document.getElementById('weather-icon'),label=document.getElementById('weather-label');if(!icon||!label)return;const wl=WL.length+WLM.length,ratio=wl>0?onlineCount/wl:0;let ic,lb;if(onlineCount===0){ic='🌙';lb='CALME';}else if(ratio<.3){ic='🌤';lb='ACTIVITÉ';}else if(ratio<.6){ic='⚡';lb='AGITÉ';}else{ic='🔥';lb='CRITIQUE';}if(icon.textContent!==ic){icon.style.transform='scale(1.4)';icon.textContent=ic;setTimeout(()=>icon.style.transform='',300);}label.textContent=lb;}
const sessionStart={};
function startSession(player){if(!sessionStart[player])sessionStart[player]=Date.now();}
function getSessionTime(player){if(!sessionStart[player])return null;const sec=Math.floor((Date.now()-sessionStart[player])/1000);if(sec<60)return sec+'s';if(sec<3600)return Math.floor(sec/60)+'min';return Math.floor(sec/3600)+'h';}
setInterval(()=>{document.querySelectorAll('.session-timer[data-player]').forEach(el=>{const t=getSessionTime(el.dataset.player);if(t)el.textContent=t;});},1000);
const sessionDurations={};
function endSession(player){if(sessionStart[player]){const dur=Math.floor((Date.now()-sessionStart[player])/1000/60);if(dur>1){if(!sessionDurations[player])sessionDurations[player]=[];sessionDurations[player].push(dur);if(sessionDurations[player].length>10)sessionDurations[player].shift();}}delete sessionStart[player];}
async function loadSessionDurations(player){try{const d=await api('/api/plages/'+encodeURIComponent(player));if(!d||!d.heatmap)return;const hm=d.heatmap;let totalSessions=0,totalDur=0;hm.forEach(dayRow=>{let streak=0,maxStreak=0;dayRow.forEach(v=>{if(v>0){streak++;maxStreak=Math.max(maxStreak,streak);}else{streak=0;}});if(maxStreak>0){totalSessions++;totalDur+=maxStreak*60;}});if(totalSessions>0&&!sessionDurations[player]?.length){sessionDurations[player]=[Math.round(totalDur/totalSessions)];}}catch(e){}}
let cwWatches=[],cwCountries={};
async function cwSave(){localStorage.setItem('mg_cw',JSON.stringify(cwWatches));}
async function cwLoadCountries(){const s=$('cw-srv').value;if(!s)return;$('cw-suggest').innerHTML=`<span>Chargement...</span>`;try{if(!cwCountries[s]){const d=await api('/api/countries/'+s);cwCountries[s]=(d.countries||[]).sort((a,b)=>a.localeCompare(b));}cwFilterCountries();}catch{$('cw-suggest').innerHTML='';}}
function cwFilterCountries(){const s=$('cw-srv').value,v=$('cw-country').value.trim().toLowerCase();const p=cwCountries[s]||[];const f=v?p.filter(x=>x.toLowerCase().includes(v)):p;$('cw-suggest').innerHTML=f.slice(0,60).map(x=>`<span class="tag" onclick="$('cw-country').value='${x.replace(/'/g,"\'")}';$('cw-acl').style.display='none';cwFilterCountries()">${x}</span>`).join('');const acl=$('cw-acl');if(v&&f.length){acl.innerHTML=f.slice(0,8).map(x=>`<div class="aci" onmousedown="$('cw-country').value='${x.replace(/'/g,"\'")}';acl.style.display='none'">${x}</div>`).join('');acl.style.display='block';}else acl.style.display='none';}
function cwAdd(){const s=$('cw-srv').value,country=$('cw-country').value.trim();if(!s||!country)return showToast('Sélectionne un serveur et un pays');const exists=cwWatches.find(w=>w.server===s&&w.country.toLowerCase()===country.toLowerCase());if(exists)return showToast('Déjà surveillé !');cwWatches.push({server:s,country,threshold:2,online:0,members:[],alertFired:false});cwSave();cwRender();cwRefreshAll();$('cw-country').value='';showToast(`${country} (${s.toUpperCase()}) ajouté`);}
async function cwRemove(idx){const w=cwWatches[idx];if(!w)return;try{await apiP('/api/country_watches/remove',{server:w.server,country:w.country});}catch(e){}cwWatches.splice(idx,1);cwSave();cwRender();showToast('Surveillance supprimée');}
async function cwRefreshOne(idx){const w=cwWatches[idx];if(!w)return;try{const d=await api(`/api/check/${w.server}/${encodeURIComponent(w.country)}`);const members=d.servers?.[w.server]||[];const online=members.length;const wasAlert=w.alertFired;w.online=online;w.members=members;w.hasNonRecruit=false;w.alertFired=false;if(online>=2){const nr=await hasNonRecruit(members,w.server);w.hasNonRecruit=nr;w.alertFired=nr;}if(w.alertFired&&!wasAlert){showPop('connect',`⚔ ${w.country}`,`${online} membres`);sendBrowserNotif('connect',`🚨 Assaut possible sur ${w.country}`,w.server);sndA(true);showToast(`🚨 ASSAUT POSSIBLE — ${w.country}`);}cwSave();}catch(e){w.online=-1;}cwRender();}
async function cwRefreshAll(){await Promise.all(cwWatches.map((_,i)=>cwRefreshOne(i)));}
function cwRender(){const el=$('cw-list');if(!el)return;if(!cwWatches.length){el.innerHTML='<div class="cw-empty">Aucun pays surveillé</div>';return;}el.innerHTML=cwWatches.map((w,i)=>{const alert=w.alertFired;const cnt=w.online<0?'?':w.online;return`<div class="cw-item ${alert?'alert':''}"><div class="cw-count ${alert?'danger':''}">${cnt}</div><div class="cw-info"><div class="cw-name">${w.country}</div><div class="cw-meta">${EMO[w.server]||''} ${w.server.toUpperCase()}</div>${w.members.length?`<div class="cw-members">${w.members.slice(0,8).map(p=>`<span>${p}</span>`).join(', ')}</div>`:''}</div><button class="btn btn-r" onclick="cwRemove(${i})">✕</button></div>`;}).join('');}
setInterval(cwRefreshAll,30000);
const gradeCache={};
async function getPlayerGrade(player,server){const key=player+'@'+server;if(gradeCache[key]&&Date.now()-gradeCache[key].ts<120000)return gradeCache[key].rank;try{const r=await fetch(`${API}/api/grade/${encodeURIComponent(player)}/${server}`);if(!r.ok)return null;const d=await r.json();gradeCache[key]={rank:d.rank,ts:Date.now()};return d.rank;}catch{return null;}}
async function hasNonRecruit(members,server){const results=await Promise.allSettled(members.slice(0,12).map(p=>getPlayerGrade(p,server)));return results.some(r=>r.status==='fulfilled'&&r.value&&r.value!==''&&r.value!=='recruit');}
function rmCalc(){const power=parseFloat(document.getElementById('rm-power').value)||0;let warzone=parseFloat(document.getElementById('rm-warzone').value)||0;const claims=parseFloat(document.getElementById('rm-claims').value)||0;if(!power)return;if(warzone>power)warzone=power;const restant=Math.round((power-warzone)*Math.pow(0.96,18));const perdu=Math.floor(power-restant);const claimsRestants=Math.max(0,Math.floor(claims-8));const diff=restant-claimsRestants;const resDiv=document.getElementById('rm-results');resDiv.style.display='block';resDiv.innerHTML=`<div><div>Power perdu: −${perdu.toLocaleString()}</div><div>Power restant: ${restant.toLocaleString()}</div><div>Claims restants: ${claimsRestants.toLocaleString()}</div><div style="color:${diff<0?'var(--red)':'var(--grn)'}">${diff<0?'⚠ SOUS-POWER : manque '+Math.abs(diff).toLocaleString():'✓ SAFE : marge '+diff.toLocaleString()}</div></div>`;}
async function doMultiSearch(){const raw=document.getElementById('ms-input').value.trim();const srv=document.getElementById('ms-srv').value;if(!raw){showToast('⚠ Entrez au moins un pseudo');return;}const names=[...new Set(raw.split('\n').map(n=>n.trim()).filter(Boolean))].slice(0,20);const res=document.getElementById('ms-results');res.innerHTML=`<div class="panel"><div class="ph"><span class="pt">⊞ Résultats — ${names.length} joueurs</span><span class="badge" id="ms-badge">...</span></div><div class="pb"><div class="ms-grid" id="ms-grid">${names.map(n=>`<div class="ms-chip" id="msc-${CSS.escape(n)}"><div class="ms-chip-name">${n}</div><div class="ld">...</div></div>`).join('')}</div></div></div>`;let found=0,done=0;const update=()=>{const el=document.getElementById('ms-badge');if(el)el.textContent=`${found} trouvés / ${done}/${names.length}`;};await Promise.all(names.map(async name=>{try{const r=await api('/api/checkall/'+encodeURIComponent(name));const chip=document.getElementById('msc-'+CSS.escape(name));if(!chip){done++;update();return;}if(r&&r.servers.length){const serverMatch=srv?r.servers.find(s=>s===srv):r.servers[0];chip.className='ms-chip found';chip.innerHTML=`<div class="ms-chip-name">${name}</div><div class="ms-chip-loc">📍 ${(serverMatch||r.servers[0]).toUpperCase()}</div>`;found++;}else{chip.className='ms-chip notfound';chip.innerHTML=`<div class="ms-chip-name">${name}</div><div class="ms-chip-no">Hors ligne</div>`;}}catch{const chip=document.getElementById('msc-'+CSS.escape(name));if(chip){chip.className='ms-chip notfound';chip.innerHTML=`<div class="ms-chip-name">${name}</div><div class="ms-chip-no">Erreur</div>`;}}done++;update();}));showToast(`⊞ Terminé — ${found}/${names.length} trouvés`);}
async function init(){const b=$('sound-btn');if(b&&!snd){b.textContent='🔇 SON';b.style.color='var(--t3)';}requestNotifPerms();api('/api/country_watches').then(d=>{cwWatches=d.watches||[];const stored=JSON.parse(localStorage.getItem('mg_cw')||'[]');stored.forEach(w=>{if(!cwWatches.find(x=>x.server===w.server&&x.country===w.country))cwWatches.push(w);});cwRender();}).catch(()=>{cwWatches=JSON.parse(localStorage.getItem('mg_cw')||'[]');cwRender();});rHist();const ok=await chkAPI();$('scan-led').className=ok?'led on':'led off';if(ok){await loadWL();await loadWLM();loadKP();await loadDash();startCountdown(5);setInterval(tickCountdown,1000);setInterval(async()=>{await loadWL();await loadDash();},5000);}}
init();
